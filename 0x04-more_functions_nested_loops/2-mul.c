#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers
 * @a: the first character to be multiplied
 * @b: the second character
 * Return: 0 if executed properly, non zero otherwise */

int mul(int a, int b)
{
	return (a * b);
}
